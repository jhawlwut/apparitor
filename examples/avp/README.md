# Amazon Verified Permissions (AVP) example

Placeholder. A later **managed/cloud** example: AVP (managed Cedar) fronted by the
[AWS open-source AuthZEN interface](https://github.com/aws-samples/sample-authzen-interface-verified-permissions)
(a Lambda that translates AuthZEN <-> AVP `IsAuthorized`). It needs an AWS account, so it
is not part of the local/CI examples. See [../README.md](../README.md).
