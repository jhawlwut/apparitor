# OpenFGA example

Placeholder. Will contain a digest-pinned OpenFGA deployment with the **experimental
AuthZEN API enabled**, a sample authorization model + relationship tuples, and a smoke
script. OpenFGA exposes AuthZEN natively (single and batch evaluation). See
[../README.md](../README.md).
